{
  "name": "ts-api-contract",
  "version": "1.0.0",
  "main": "build",
  "types": "build",
  "files": [
    "build/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "cd test && npm test"
  },
  "devDependencies": {
    "typescript": "^3.3.4000"
  }
}
